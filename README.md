<h3 align="center">Kanastra's Backend Challenge</h3>

### Prerequisites

* If your system supports Makefile, just run:
  ```sh
  make dev
  ```
* Otherwise run:
  ```sh
  docker-compose up --build
  ```
